// Canonical public URL — one source of truth for metadata, sitemap, robots, and .ics links.
// Domain registered near launch; override via NEXT_PUBLIC_SITE_URL when deploying to a preview host.
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL || 'https://kickoff26.app';
